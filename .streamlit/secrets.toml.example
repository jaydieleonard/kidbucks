# Copy this file to `.streamlit/secrets.toml` and fill in your real values.
# secrets.toml is git-ignored so your password never gets committed.
#
# On Streamlit Community Cloud, paste the same line into
# App → Settings → Secrets (do NOT commit it to the repo).

DATABASE_URL = "postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require"
