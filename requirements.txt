# Pinned to the 1.58 line: we use st.components.v1.html to remember the family
# on the device (set a cookie/localStorage), which later Streamlit versions drop.
streamlit>=1.58,<1.59
pandas
psycopg[binary]>=3.1
streamlit-cookies-controller>=0.0.4
